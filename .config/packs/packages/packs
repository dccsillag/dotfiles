# vim: ft=sh

check() {
    command_exists packs
}

install_manual() {
    git clone git@github.com:dccsillag/packs.git
    cd packs
    cp packs.sh ~/.local/bin/packs
    chmod +x ~/.local/bin/packs
}
