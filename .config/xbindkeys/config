"for SRC in $(pactl list sources short | cut -f 1); do pacmd set-source-mute $SRC 0; done"
  c:169

"for SRC in $(pactl list sources short | cut -f 1); do pacmd set-source-mute $SRC 1; done"
  release+c:169

"for SRC in $(pactl list sources short | cut -f 1); do pacmd set-source-mute $SRC 0; done"
  shift+c:169
