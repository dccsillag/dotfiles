"for SRC in $(pactl list sources short | grep -v monitor | cut -f 1); do pacmd set-source-mute $SRC 0; done"
  c:86

"for SRC in $(pactl list sources short | grep -v monitor | cut -f 1); do pacmd set-source-mute $SRC 1; done"
  release+c:86

"for SRC in $(pactl list sources short | grep -v monitor | cut -f 1); do pacmd set-source-mute $SRC 0; done"
  shift+c:86

"mmc toggle"
  b:8
